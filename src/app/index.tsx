import { View, Text } from "@/tw";

export default function Index() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-h1 font-bold text-primary">Lingua</Text>
    </View> 
  );
}